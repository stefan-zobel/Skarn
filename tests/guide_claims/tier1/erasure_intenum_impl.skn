// CLAIM: an integer-backed enum erases at runtime and cannot implement a trait.
// GUIDE: Representation erasure; §24
// EXPECT: fail cannot implement trait
trait Greet { fn greet(self) -> String }
enum Color : Int { Red, Green }
impl Greet for Color { fn greet(self) -> String { "c" } }
println(1)
