// CLAIM: intoIter lifts a Vec into a dyn Iterator so combinators apply.
// GUIDE: §19 iterators
// EXPECT: ok 3
fn f(x: Int) -> Int { x + 1 }
println(len(toVec([1, 2, 3]) |> intoIter |> map(f) |> collect))
