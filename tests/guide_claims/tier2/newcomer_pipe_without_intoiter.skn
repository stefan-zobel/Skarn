// CLAIM: newcomer diagnostics -- piping a Vec into `map` explains that a container must be lifted with `intoIter`.
// GUIDE: SkarnIn30Minutes.md (the everyday surface); tests/guide_claims/README.md
// EXPECT: fail lift it with `intoIter`
let xs = toVec([1, 2, 3])
let ys = xs |> map(fn(x) { x * 2 }) |> collect
println(len(ys))
